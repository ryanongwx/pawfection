import 'package:flutter/material.dart';

class VTaskScreen extends StatelessWidget {
  const VTaskScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
