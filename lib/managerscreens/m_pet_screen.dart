import 'package:flutter/material.dart';

class MPetScreen extends StatelessWidget {
  const MPetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
